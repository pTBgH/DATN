
# =============================================================================
# Archives / build artifacts / backups (cleanup PR — A.2)
*.zip
*.tar.gz
*.bak.*
*-result.txt
# =============================================================================
# Cilium policy dry-run dumps
*.dryrun.yaml
.DS_Store
# Editor / OS noise
.env
**/.env.development.local
**/.env.local
evidence/audit-*/
evidence/baseline-*/
evidence/microseg-conformance-*
# Evidence / baseline directories from PR #7 observability tool
# Generated by infras/k8s-yaml/vault-scripts/99-fast-rebuild-vault.sh.
# Generated TLS / token / kubeconfig artifacts
.idea/
infras/cosign-keys/*.key
infras/cosign-keys/*.key.old.*
infras/cosign-keys/*.pub.old.*
infras/cosign-keys/zta.key
infras/k8s-yaml/vault-scripts/vault-prod-init.json
# Local environment files (Laravel + frontends)
*.log
# Misc runtime
node_modules/
*.pid
# PR #16 — Cosign private signing keys (NEVER commit)
# Pre-existing entry retained:
# Repo-level .gitignore — DATN Zero Trust project
*.swp
**/vault-ca.crt
# Vault initialization output (root token + recovery keys) — F-2 audit fix.
**/vault-init-*.json
**/vault-prod-init.json
# (vault-scripts/.gitignore already has vault-prod-init.json)
**/vault-tls-*.pem
**/.vault-token
vendor/
.vscode/
infras/k8s-yaml/*-patched.yaml
