% !TEX encoding = UTF-8
\chapter{Tổng quan về kiến trúc Zero Trust}

%==============================================================================
% 1.1  TỪ BẢO MẬT VÀNH ĐAI ĐẾN ZERO TRUST
%==============================================================================
\section{Từ bảo mật vành đai đến Zero Trust}

\subsection{Hạn chế của mô hình bảo mật dựa trên chu vi}

Trong nhiều thập kỷ, an ninh mạng truyền thống được xây dựng theo mô hình \textbf{phòng thủ dựa trên chu vi}. Mô hình này thiết lập ranh giới giữa mạng nội bộ và bên ngoài thông qua tường lửa, VPN và hệ thống IDS/IPS. Đặc trưng của mô hình này là cấp quyền truy cập chủ yếu dựa trên vị trí mạng \parencite{kindervag2010build,jericho2004}. 

Mặc dù mạng truyền thống vẫn tích hợp một số biện pháp phòng vệ nội bộ (chia VLAN, tường lửa nội bộ, các cơ chế định danh tĩnh ở tầng ứng dụng), mô hình này bộc lộ ba hạn chế khi đối mặt với hạ tầng cloud-native (thiết kế tối ưu cho môi trường đám mây) hiện đại:

\begin{itemize}
    \item \textbf{Sự tin cậy ngầm định:} Các hệ thống an ninh truyền thống thường mặc định tin cậy thiết bị và luồng dữ liệu xuất phát từ bên trong mạng. Theo \parencite{nist800207}, việc cấp quyền truy cập dựa trên vị trí mạng dẫn đến rủi ro: một khi kẻ tấn công hoặc tác nhân nội bộ vượt qua được ranh giới mạng, các hoạt động của họ bên trong hệ thống thường không bị kiểm tra thêm.
    
    \item \textbf{Di chuyển ngang:} Các biện pháp kiểm soát mạng nội bộ truyền thống chủ yếu dựa trên việc phân chia theo mạng con (VLAN/Subnet). Do thiếu cơ chế kiểm soát truy cập chi tiết, khi một điểm nút bị chiếm quyền, kẻ tấn công có thể tái sử dụng các danh tính tĩnh (như token có thời hạn dài) để quét mạng và kết nối đến các tài nguyên khác trong cùng phân đoạn mạng trước khi bị hệ thống phát hiện \parencite{kindervag2010build,gilman2017zero}.
    
    \item \textbf{Sự mờ nhạt của ranh giới mạng:} Môi trường điện toán đám mây, xu hướng làm việc từ xa và việc sử dụng thiết bị cá nhân cho công việc làm cho ranh giới mạng vật lý của tổ chức không còn xác định rõ ràng. Theo định hướng của CISA, việc thiết lập một "chu vi" cố định để đặt thiết bị kiểm soát (như tường lửa) không còn đáp ứng được yêu cầu khi tài nguyên và người dùng bị phân tán \parencite{cisa_ztmm2023,nist800207}.
\end{itemize}

\subsection{Khái niệm và định nghĩa về Zero Trust}

Đối mặt với những hạn chế của mô hình an ninh mạng dựa trên ranh giới truyền thống, thuật ngữ Zero Trust được John Kindervag đề xuất vào năm 2010 tại Forrester Research \parencite{kindervag2010build}. Cách tiếp cận này kế thừa tư tưởng phi chu vi của Jericho Forum \parencite{jericho2004} và sau đó được Viện Tiêu chuẩn và Công nghệ Quốc gia Hoa Kỳ (NIST) chuẩn hóa thành các định nghĩa chính thức trong tài liệu NIST SP 800-207 \parencite{nist800207}.

% Sử dụng môi trường Định nghĩa đã khai báo trong preamble
\begin{definition}[Zero Trust]
Zero Trust là một tập hợp các nguyên lý an ninh mạng phát triển dựa trên triết lý: không bao giờ tồn tại sự tin cậy ngầm định đối với bất kỳ tài sản hay tài khoản người dùng nào chỉ dựa trên vị trí mạng vật lý. Thay vào đó, mọi yêu cầu truy cập đều phải được xác thực, đánh giá liên tục theo ngữ cảnh và chỉ được cấp mức đặc quyền tối thiểu cần thiết để thực hiện nhiệm vụ \parencite{nist800207}.
\end{definition}

\begin{definition}[Kiến trúc Zero Trust]
Kiến trúc Zero Trust là kế hoạch an ninh mạng của một tổ chức ứng dụng các nguyên tắc Zero Trust. Kiến trúc Zero Trust bao gồm việc thiết kế các thành phần hạ tầng, cấu trúc luồng công việc và chính sách kiểm soát nhằm hiện thực hóa các cơ chế xác thực, ủy quyền và giám sát trong môi trường thực tế \parencite{nist800207}.
\end{definition}

Nói một cách ngắn gọn, Zero Trust là một triết lý và hệ tư tưởng về an toàn thông tin, trong khi kiến trúc Zero Trust là bản thiết kế kỹ thuật để đưa triết lý đó vào vận hành thực tiễn.

% Từ các tenet và mô hình logic được trình bày trong NIST SP 800-207, có thể rút ra hai tư tưởng xuyên suốt mọi triển khai Zero Trust \parencite{nist800207,gilman2017zero}:
% \begin{enumerate}
%     \item \textbf{Mạng luôn bị coi là đã bị xâm nhập}.
%     \item \textbf{Mọi truy cập đều phải được xác thực và uỷ quyền} một cách rõ ràng trên cơ sở \textit{từng yêu cầu}, không được phụ thuộc vào vị trí mạng của chủ thể.
% \end{enumerate}

\subsection{So sánh kiến trúc Zero Trust với mô hình dựa trên chu vi}

Để làm rõ lý do tại sao ZTA là sự lựa chọn tất yếu cho môi trường công nghệ thông tin hiện đại, Bảng~\ref{tab:zta_vs_perimeter} so sánh sự khác biệt về triết lý và phương pháp tiếp cận kỹ thuật giữa hai mô hình:

\subsection{So sánh kiến trúc Zero Trust với mô hình dựa trên chu vi}

Bảng~\ref{tab:zta_vs_perimeter} trình bày tóm tắt sự khác biệt về phương pháp tiếp cận kỹ thuật và quản lý rủi ro giữa mô hình bảo mật mạng truyền thống và kiến trúc Zero Trust.

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3}
\caption{So sánh mô hình bảo mật truyền thống và kiến trúc Zero Trust}
\label{tab:zta_vs_perimeter}
\begin{tabularx}{\textwidth}{|p{3.5cm}|X|X|}
\hline
\textbf{Tiêu chí} & \textbf{Bảo mật truyền thống} & \textbf{Kiến trúc Zero Trust} \\
\hline
\textbf{Cơ sở tin cậy} & Xác thực một lần tại biên mạng. Mức độ tin cậy phụ thuộc vào dải mạng (IP, subnet). & Không phụ thuộc vị trí mạng. Đánh giá độ tin cậy liên tục dựa trên ngữ cảnh từ các nguồn dữ liệu. \\
\hline
\textbf{Mức độ kiểm soát} & Tập trung tại ranh giới mạng, chia hệ thống thành các vùng mạng có quy mô lớn. & Phân chia mạng ở mức độ chi tiết, áp dụng kiểm soát trực tiếp tại từng tài nguyên hoặc ứng dụng. \\
\hline
\textbf{Kiểm soát di chuyển ngang} & Dựa vào các quy tắc mạng tĩnh. Khó phát hiện hành vi dò quét và di chuyển khi tác nhân đã lọt vào mạng nội bộ. & Áp dụng nguyên tắc từ chối mặc định. Truy cập chỉ được phép thông qua xác thực định danh mật mã học và tuân thủ nguyên tắc đặc quyền tối thiểu. \\
\hline
\textbf{Quản lý thông tin xác thực} & Sử dụng thông tin xác thực tĩnh, có thời gian sống dài hoặc vô hạn. & Cấp phát thông tin xác thực động  với thời gian sống ngắn, được thu hồi tự động. \\
\hline
\textbf{Khả năng giám sát} & Dữ liệu giám sát chủ yếu thu thập từ lưu lượng tại các thiết bị mạng biên (tường lửa, router). & Thu thập nhật ký và số liệu toàn diện tại mọi điểm kiểm soát để phục vụ cơ chế phản hồi an ninh. \\
\hline
\end{tabularx}
\end{table}
% khả năng giám sát or khả năng quan sát?

%==============================================================================
% 1.2  KIẾN TRÚC THAM CHIẾU ZERO TRUST THEO NIST SP 800-207
%==============================================================================
\section{Kiến trúc tham chiếu Zero Trust}

\subsection{Bảy nguyên lý}

NIST SP 800-207 định nghĩa 7 nguyên lý nền tảng mà mọi triển khai kiến trúc Zero Trust phải tuân thủ \parencite{nist800207}:

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3} % Giãn dòng cho bảng dễ đọc
\caption{Bảy nguyên lý Zero Trust}
\label{tab:seven_tenets}
\begin{tabularx}{\textwidth}{|c|p{4.5cm}|X|}
\hline
\textbf{\#} & \textbf{Nguyên lý} & \textbf{Nội dung} \\
\hline
1 & Mọi thứ đều là tài nguyên & Máy chủ, ứng dụng, API endpoint, cơ sở dữ liệu (CSDL) đều được coi là tài nguyên độc lập cần bảo vệ. \\ 
\hline
2 & Bảo mật không phụ thuộc vị trí mạng & Không có vùng mạng nào được tin cậy mặc định. Giao tiếp nội bộ phải được kiểm soát và xác thực với mức độ nghiêm ngặt tương đương lưu lượng từ bên ngoài. \\
\hline
3 & Cấp quyền theo phiên & Quyền truy cập được cấp theo từng phiên cụ thể với đặc quyền tối thiểu và thu hồi ngay khi phiên kết thúc. \\
\hline
4 & Chính sách truy cập động & Quyết định truy cập được đánh giá liên tục dựa trên sự kết hợp của nhiều yếu tố: danh tính, trạng thái thiết bị, hành vi người dùng, thời gian và vị trí. \\
\hline
5 & Giám sát toàn bộ tài sản & Tổ chức phải liên tục giám sát và đánh giá tính toàn vẹn, cũng như trạng thái tuân thủ bảo mật của mọi thiết bị và ứng dụng. \\
\hline
6 & Xác thực liên tục & Việc xác thực và ủy quyền không chỉ diễn ra ở lần kết nối đầu tiên mà là một tiến trình liên tục, đặc biệt trước các thao tác nhạy cảm. \\
\hline
7 & Thu thập tối đa dữ liệu & Thu thập dữ liệu trạng thái về tài sản, hạ tầng mạng và truyền thông để làm đầu vào cho việc ra quyết định và cải thiện tư thế an ninh. \\
\hline
\end{tabularx}
\end{table}

\subsection{Các thành phần trong kiến trúc tham chiếu}
\label{subsec:logic_pdp_pep_pip}

NIST SP 800-207 định nghĩa kiến trúc logic ZTA tách biệt hoàn toàn giữa \textbf{mặt phẳng điều khiển (control plane)} và \textbf{mặt phẳng dữ liệu (data plane)} \parencite{nist800207}. NIST SP 1800-35 bổ sung và sử dụng nhất quán bốn vai trò kiến trúc dưới đây, trong đó PE và PA hợp thành \textbf{Policy Decision Point (PDP)} ở control plane, còn PEP thực thi tại data plane và PIP cấp ngữ cảnh cho PDP.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/nist_zta_logical.png}
    \caption[Kiến trúc tham chiếu tổng quát của ZTA]{Kiến trúc tham chiếu tổng quát của ZTA \parencite{nist_sp_1800_35}.}
    \label{fig:nist_zta_logical}
\end{figure}

\subsection{Policy Engine (PE) -- Bộ xử lý chính sách}
Chịu trách nhiệm ra quyết định cuối cùng về việc cấp, từ chối hoặc thu hồi quyền truy cập đối với tài nguyên. PE sử dụng thuật toán tin cậy (trust algorithm) để đánh giá rủi ro của từng yêu cầu.

\subsection{Policy Administrator (PA) -- Bộ điều phối chính sách}
Đóng vai trò thực thi quyết định của PE bằng cách gửi mệnh lệnh đến PEP để thiết lập hoặc ngắt luồng giao tiếp giữa chủ thể và tài nguyên.

\subsection{Policy Enforcement Point (PEP) -- Điểm thực thi chính sách}
Nằm trực tiếp trên đường truyền dữ liệu, có nhiệm vụ bảo vệ vùng tin cậy chứa tài nguyên, đồng thời kích hoạt, giám sát và kết thúc các kết nối theo mệnh lệnh từ PA.

\subsection{Policy Information Point (PIP) -- Điểm cung cấp thông tin chính sách}
Bao gồm các hệ thống hỗ trợ cung cấp phép đo từ xa và dữ liệu ngữ cảnh (như định danh, phân tích bảo mật, tình báo mối đe dọa) cho PDP, giúp PE có cơ sở để cập nhật quyết định liên tục.

\subsubsection{Cơ chế ra quyết định (Thuật toán tin cậy)}
\label{subsubsec:trust_algorithm}

Trong cấu trúc trên, Bộ xử lý chính sách (PE) đóng vai trò trung tâm phân tích. Tiến trình đánh giá và ra quyết định của PE dựa trên dữ liệu đầu vào từ PIP được chuẩn NIST SP 800-207 định nghĩa là \textit{thuật toán tin cậy} (trust algorithm - TA). Các biến thể của thuật toán này được phân loại dựa trên hai phương diện độc lập \parencite{nist800207}:

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.4} 
\caption{Phân loại các phương pháp tiếp cận của thuật toán tin cậy trong kiến trúc Zero Trust}
\label{tab:trust_algorithm}
\begin{tabularx}{\textwidth}{|p{4cm}|X|}
\hline
\textbf{Phương diện phân loại} & \textbf{Các hướng tiếp cận tiêu biểu} \\
\hline

\textbf{Cơ sở ra quyết định}
& 
\textbf{1. Dựa trên chính sách/thuộc tính:} Quyết định cấp quyền được thực hiện khi chủ thể thỏa mãn một tập hợp các quy tắc hoặc điều kiện đã được cấu hình từ trước. \newline
\textbf{2. Dựa trên điểm số/rủi ro:} PE tính toán điểm tin cậy hoặc mức độ rủi ro dựa trên trọng số của nhiều yếu tố ngữ cảnh (như định danh, trạng thái thiết bị, hành vi, tình báo an ninh). Quyết định truy cập được đưa ra bằng cách so sánh điểm số này với ngưỡng quy định. \\
\hline

\textbf{Mức độ sử dụng ngữ cảnh}
& 
\textbf{1. Đánh giá độc lập:} Mỗi yêu cầu truy cập được xử lý như một sự kiện rời rạc. Thuật toán không (hoặc rất ít) xem xét đến lịch sử dữ liệu của các phiên trước đó. \newline
\textbf{2. Đánh giá liên tục:} Quyết định truy cập được đánh giá và cập nhật động trong suốt vòng đời của phiên. Hệ thống liên tục theo dõi lịch sử hành vi, sự thay đổi trạng thái bảo mật hoặc các tín hiệu rủi ro mới để duy trì hoặc thu hồi đặc quyền. \\
\hline

\end{tabularx}
\end{table}



\subsection{Các hướng tiếp cận triển khai kiến trúc Zero Trust}
\label{subsec:zta_deployment_models}

Tùy thuộc vào đặc thù hạ tầng, các tổ chức có thể lựa chọn các phương pháp khác nhau để hiện thực hóa mô hình logic của kiến trúc Zero Trust. Hai tài liệu tiêu chuẩn NIST SP 800-207 và SP 1800-35 phân loại bốn hướng tiếp cận triển khai phổ biến như sau \parencite{nist800207,nist_sp_1800_35}:

\begin{itemize}[itemsep=6pt, parsep=0pt]
    \item \textbf{Quản trị danh tính nâng cao:} Lấy định danh làm trung tâm. Quyết định truy cập hoàn toàn phụ thuộc vào việc xác minh chủ thể là ai và có các thuộc tính gì, thay vì dựa vào thiết bị hay vị trí mạng.
    
    \item \textbf{Phân đoạn vi mô:} Lấy mạng làm trung tâm. Phương pháp này chia nhỏ hệ thống thành các vùng mạng độc lập, bao bọc lấy từng tài nguyên riêng lẻ nhằm cô lập rủi ro và chặn đứng các hành vi di chuyển ngang.
    
    \item \textbf{Chu vi định nghĩa bằng phần mềm:} Lấy ứng dụng làm trung tâm. Hạ tầng và tài nguyên được ẩn giấu hoàn toàn khỏi môi trường mạng, chỉ hiển thị và cho phép thiết lập kết nối sau khi chủ thể đã được hệ thống xác thực và ủy quyền hợp lệ.
    
    \item \textbf{Biên dịch vụ truy nhập bảo mật:} Lấy mạng biên làm trung tâm. Hướng đi này hợp nhất điểm thực thi chính sách với các nền tảng bảo mật đám mây, đưa hệ thống kiểm soát đến vị trí gần với thiết bị của người dùng nhất có thể.
\end{itemize}

Bốn hướng tiếp cận triển khai nêu trên không loại trừ lẫn nhau. Trên thực tế, một kiến trúc Zero Trust toàn diện thường là sự giao thoa, chứa đựng các thành phần của nhiều hướng tiếp cận khác nhau tùy thuộc vào từng bài toán thiết kế hạ tầng cụ thể \parencite{nist800207}.

%==============================================================================
% 1.3  CÁC KHÁI NIỆM MỞ RỘNG CỦA ZERO TRUST
%==============================================================================
% \section{Các khái niệm mở rộng của Zero Trust}

% \subsection{Bảo mật thích ứng và CAEP}
% \label{subsec:adaptive_caep}

% Khi Policy Engine sử dụng thuật toán tin cậy theo ngữ cảnh, quyết định cấp truy cập không kết thúc tại thời điểm cấp token. Vòng đời truy cập trong ZTA là một vòng đời \emph{đánh giá liên tục}, trong đó PIP tiếp tục đẩy tín hiệu mới (thiết bị bị xâm nhập, hành vi bất thường) tới PDP để \textit{thu hồi quyền truy cập giữa phiên}.

% NIST gọi mô hình này là \textbf{Bảo mật thích ứng (adaptive security)} \parencite{nist800207}. Một cách triển khai cụ thể của tầng này là \textbf{Continuous Access Evaluation Profile (CAEP)} \parencite{openid_caep} -- đặc tả do OpenID Foundation duy trì, mô tả mô hình \textit{hướng sự kiện} cho phép IdP phát tín hiệu thay đổi ngữ cảnh tới các PEP để ngắt phiên ngay cả khi token hiện tại chưa hết hạn TTL. Điểm đáng chú ý là vòng lặp này \textit{không đòi hỏi PEP kiểm tra IdP} mà hoạt động theo cơ chế Push.

% \subsection{Tiếp cận đa thành phần}
% \label{subsec:no_single_product}

% NIST SP 800-207 mô tả ZTA bằng các vai trò logic, không gắn vai trò với một sản phẩm thương mại cụ thể~\parencite{nist800207}. Việc gán toàn bộ nền tảng ZTA cho một sản phẩm duy nhất thường dẫn đến \textit{phụ thuộc nhà cung cấp} và không phủ đầy đủ các vai trò bảo mật (ví dụ: một giải pháp mạng tốt có thể không cung cấp khả năng đánh giá trạng thái bảo mật của ứng dụng).

% Do đó, xu hướng hiện đại hướng tới tổ hợp \textbf{Đa thành phần} -- chọn công nghệ phù hợp nhất cho từng vai trò logic (PE, PA, PEP, PIP) và liên kết chúng qua các tiêu chuẩn mở (OIDC, SPIFFE, OpenTelemetry). Đây là phương pháp được áp dụng nhất quán trong việc thiết kế kiến trúc hệ thống của đồ án này.


%==============================================================================
% 1.4  MÔ HÌNH TRƯỞNG THÀNH ZERO TRUST (ZTMM)
%==============================================================================
\section{Mô hình trưởng thành Zero Trust}

Bên cạnh kiến trúc tham chiếu của NIST, Cơ quan An ninh Mạng và Cơ sở Hạ tầng Hoa Kỳ (CISA) cung cấp một lộ trình thực tiễn dưới dạng Mô hình trưởng thành Zero Trust (Zero Trust Maturity Model - ZTMM) \parencite{cisa_ztmm2023}. Mô hình này đánh giá Zero Trust không phải là một trạng thái thiết lập tức thời, mà là một tiến trình nâng cấp liên tục, được cấu trúc dựa trên 5 trụ cột chức năng, 3 năng lực xuyên suốt và 4 cấp độ trưởng thành.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/ZTAPillars.png}
    \caption[Mô hình trưởng thành Zero Trust của CISA]{Mô hình trưởng thành Zero Trust của CISA \parencite{cisa_ztmm2023}.}
    \label{fig:ZTAPillars}
\end{figure}

\subsection{Năm trụ cột chức năng và ba năng lực xuyên suốt}

Mô hình phân chia hệ thống an ninh thành 5 trụ cột chức năng độc lập nhưng có khả năng tương tác:
\begin{itemize}[itemsep=6pt, parsep=0pt]
    \item \textbf{Danh tính (Identity):} Hợp nhất hệ thống quản lý, thực thi xác thực đa yếu tố và đánh giá rủi ro danh tính liên tục.
    \item \textbf{Thiết bị (Devices):} Quản lý, kiểm kê và liên tục xác minh tình trạng tuân thủ bảo mật của thiết bị yêu cầu truy cập.
    \item \textbf{Mạng lưới (Networks):} Quản lý luồng lưu lượng động, cô lập tài nguyên qua kỹ thuật phân đoạn vi mô và mã hoá truyền thông.
    \item \textbf{Ứng dụng và hệ thống tính toán (Applications \& Workloads):} Tích hợp kiểm soát bảo mật vào quy trình triển khai, đánh giá ủy quyền liên tục đối với các luồng gọi ứng dụng.
    \item \textbf{Dữ liệu (Data):} Phân loại, dán nhãn, mã hoá dữ liệu ở mọi trạng thái và ứng dụng cơ chế phòng chống thất thoát.
\end{itemize}

Để duy trì 5 trụ cột trên, hệ thống cần thiết lập 3 năng lực nền tảng xuyên suốt:
\begin{itemize}[itemsep=6pt, parsep=0pt]
    \item \textbf{Tầm nhìn và phân tích:} Thu thập và phân tích dữ liệu trên toàn bộ môi trường để làm cơ sở cho các quyết định truy cập.
    \item \textbf{Tự động hóa và điều phối:} Sử dụng công cụ để chuyển đổi các quy trình phản ứng thủ công thành hành động tự động, đảm bảo tính nhất quán trên quy mô lớn.
    \item \textbf{Quản trị:} Thiết lập và duy trì các chính sách an ninh mạng tập trung nhằm đảm bảo sự tuân thủ trên toàn hệ thống.
\end{itemize}

\subsection{Bốn cấp độ trưởng thành}

Để tổ chức có thể đánh giá mức độ hoàn thiện của hệ thống hiện tại, CISA chia lộ trình chuyển đổi thành 4 cấp độ \parencite{cisa_ztmm2023}:
\begin{itemize}[itemsep=6pt, parsep=0pt]
    \item \textbf{Truyền thống (Traditional):} Dựa vào xác thực tĩnh, phân đoạn mạng quy mô lớn và thiết lập các ranh giới tin cậy cố định.
    \item \textbf{Khởi tạo (Initial):} Bắt đầu tự động hoá các quy trình cơ bản, phân quyền dựa trên thuộc tính và bước đầu nâng cao khả năng quan sát hệ thống nội bộ.
    \item \textbf{Nâng cao (Advanced):} Cấu hình hệ thống tự động hóa, thực thi chính sách bảo mật phối hợp chéo giữa các trụ cột và phản hồi an ninh dựa trên đánh giá rủi ro.
    \item \textbf{Tối ưu (Optimal):} Đạt trạng thái hệ thống tự động hoàn toàn, áp dụng công nghệ phân tích hành vi để cấp quyền hoặc thu hồi truy cập động theo ngưỡng rủi ro theo thời gian thực.
\end{itemize}


%==============================================================================
% 1.5 KẾT LUẬN CHƯƠNG 1
%==============================================================================
\section{Kết luận chương 1}

Chương 1 đã hệ thống hóa các lý thuyết nền tảng về Zero Trust dựa trên tiêu chuẩn của NIST và mô hình đánh giá của CISA. Về cơ bản, phương pháp tiếp cận này cung cấp một khuôn khổ an toàn thông tin toàn diện nhằm loại bỏ hoàn toàn sự tin cậy ngầm định tồn tại trong mạng truyền thống.

Bước tiếp theo là đưa khuôn khổ lý thuyết trên vào ứng dụng thực tiễn. Do hệ thống Microservices sở hữu cấu trúc phân tán với nhiều đặc thù riêng về mặt vận hành, Chương 2 sẽ tập trung phân tích bề mặt tấn công của nền tảng này, từ đó đề xuất một giải pháp bảo mật phù hợp và hiệu quả.
