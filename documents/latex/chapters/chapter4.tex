% !TEX encoding = UTF-8
\chapter{Thử nghiệm và Đánh giá}

\section{Mục tiêu chương}

Chương này trình bày kế hoạch thử nghiệm và khung đánh giá cho giai đoạn tiếp theo của đồ án, nhằm kiểm chứng mức độ hiệu quả của kiến trúc Zero Trust đã triển khai trên hệ thống JOB7189. Trọng tâm là chứng minh mối liên hệ giữa từng cơ chế kiểm soát và các nguyên lý (tenets) trong NIST SP 800-207 \cite{nist800207}.

\section{Kịch bản thử nghiệm}

Mỗi kịch bản được thiết kế để xác thực một nhóm nguyên lý cụ thể của NIST SP 800-207.

\subsection{Kịch bản 1: Lateral Movement}

\textbf{Mục tiêu:} Pod bị chiếm quyền cố gắng truy cập dịch vụ ngoài chính sách cho phép.

\textbf{Ý nghĩa kiểm chứng:}
\begin{itemize}
    \item Least Privilege (đặc quyền tối thiểu).
    \item Micro-segmentation với nguyên tắc Deny-All, Allow-Explicit.
\end{itemize}

\subsection{Kịch bản 2: API Abuse}

\textbf{Mục tiêu:} Gọi sai endpoint hoặc sai HTTP method và bị Cilium L7 policy chặn.

\textbf{Ý nghĩa kiểm chứng:}
\begin{itemize}
    \item Per-request Access Decision (quyết định truy cập theo từng yêu cầu).
\end{itemize}

\subsection{Kịch bản 3: Runtime Anomaly}

\textbf{Mục tiêu:} Phát sinh hành vi bất thường trong Pod (ví dụ thực thi lệnh hệ thống trái phép).

\textbf{Ý nghĩa kiểm chứng:}
\begin{itemize}
    \item Continuous Monitoring (giám sát liên tục).
\end{itemize}

\textit{Lưu ý:} Kịch bản này sẽ được hoàn thiện khi lớp Runtime Enforcement (Tetragon) được triển khai đầy đủ (bổ sung sau).

\subsection{Kịch bản 4: Credential Reuse}

\textbf{Mục tiêu:} Vault credential hết hạn TTL, bị thu hồi, và kiểm tra khả năng chống tái sử dụng thông tin xác thực cũ.

\textbf{Ý nghĩa kiểm chứng:}
\begin{itemize}
    \item Dynamic Trust Evaluation (đánh giá tin cậy động theo thời gian).
\end{itemize}

\section{Phân tích kết quả}

Khung phân tích kết quả dự kiến gồm ba lớp:
\begin{itemize}
    \item \textbf{Định lượng giảm attack surface:} so sánh số attack path khả thi trước và sau khi áp dụng ZTA.
    \item \textbf{Đánh giá blast radius:} đo số dịch vụ mà một Pod bị compromise có thể reach trước và sau policy.
    \item \textbf{Đối chiếu Traceability Matrix:} ánh xạ theo chuỗi \textit{threat $\rightarrow$ control $\rightarrow$ result} để chứng minh tính giải thích được của từng kết quả thử nghiệm.
\end{itemize}

\section{Đánh giá hiệu năng}

Nội dung đánh giá hiệu năng tập trung vào các chỉ số trọng yếu:
\begin{itemize}
    \item \textbf{Latency P99:} so sánh trước/sau khi áp dụng Network Policy và mTLS.
    \item \textbf{Resource overhead:} mức tiêu thụ tài nguyên của Cilium, Tetragon, Vault Agent.
    \item \textbf{Trade-off bảo mật - hiệu năng:} phân tích mức tăng độ trễ và tài nguyên đổi lấy mức giảm rủi ro bảo mật.
    \item \textbf{Ngưỡng CPU tối thiểu:} xác định số vCPU cần thiết để Phase 4 enforcement chạy ổn định, làm cơ sở khuyến nghị cấu hình triển khai.
\end{itemize}

\subsection{Kịch bản thực nghiệm: Ngưỡng CPU và leader-election cascade}
\label{subsec:cpu_starvation_eval}

Kịch bản này định lượng giả thuyết được trình bày ở Chương~3 (mục~\ref{sec:resource_budget_lessons}): với cùng một code base và cùng một kịch bản triển khai \texttt{zta-rebuild.sh --yes --full-enforcement}, biến số duy nhất là số vCPU cấp cho host VM.

\paragraph{Cấu hình thử nghiệm.} Host VMware Workstation, RAM 8\,GiB, ổ SSD, image \texttt{kindest/node:v1.35.0}, Cilium 1.19, Kubernetes v1.35.0. Hai cấu hình CPU được khảo sát:

\begin{itemize}
    \item \textbf{Cấu hình A:} 2 vCPU (AMD Ryzen 5 PRO 4650U).
    \item \textbf{Cấu hình B:} 6 vCPU (cùng CPU vật lý, cấp thêm core qua VMware).
\end{itemize}

Pre-flight gate \texttt{ZTA\_HOST\_RAM\_CHECK\_FATAL=0} được tắt ở cả hai cấu hình để pipeline thực sự chạy đến hết phase \texttt{full-enforcement} và quan sát hành vi runtime, thay vì bị chặn ngay từ đầu.

\paragraph{Quan sát ở Cấu hình A (2 vCPU).} Sau khi pipeline hoàn tất các phase nặng, cluster không đạt trạng thái ổn định. Bảng~\ref{tab:restart_2vcpu} liệt kê số lần restart đo bằng \texttt{kubectl get pod -A} sau khoảng 60--66 phút kể từ khi tạo cluster.

\begin{table}[H]
\centering
\caption{Restart đồng pha của control-plane và workload có leader-election ở cấu hình 2 vCPU}
\label{tab:restart_2vcpu}
\begin{tabularx}{\textwidth}{|p{4.5cm}|p{2.5cm}|X|}
\hline
\textbf{Pod} & \textbf{Số restart} & \textbf{Ghi chú} \\
\hline
\texttt{kube-controller-manager} & 5 & Restart cuối $\sim$76\,s trước thời điểm chụp \\
\hline
\texttt{kube-scheduler} & 4 & Restart cuối $\sim$70\,s, đồng pha trong $<10\,$s \\
\hline
\texttt{cilium-operator} & 5 & Liveness probe \texttt{127.0.0.1:9234} báo \texttt{connection refused} \\
\hline
\texttt{spire-server-0} (sidecar \texttt{spire-controller-manager}) & 4 & Cùng đợt restart với 3 thành phần trên \\
\hline
\texttt{policy-controller-webhook} & 4 & Webhook không leader-election nhưng restart do CPU thiếu \\
\hline
8 microservices Laravel & 4--5 mỗi service & Pod \texttt{4/4 Ready} nhưng restart định kỳ \\
\hline
\texttt{vault-agent-injector}, \texttt{kube-state-metrics} & 2 & Tần suất thấp hơn nhưng vẫn restart \\
\hline
\end{tabularx}
\end{table}

Đồng thời, tuổi đời các pod cilium-agent (DaemonSet 4 instance) lệch nhau theo bậc 3--4 phút (17m, 14m, 8m30s, 3m41s tại thời điểm chụp), trong khi mỗi pod đều có \texttt{RESTARTS = 0}. Đây là dấu hiệu DaemonSet bị \texttt{rollout restart} tuần hoàn, kích hoạt từ phase \texttt{harden} khi bật \texttt{mesh-auth-enabled=true} và \texttt{enable-wireguard=true}. Mỗi lần một cilium-agent bước qua giai đoạn \texttt{Init:5/6} (load lại eBPF program, mount \texttt{/sys/fs/bpf}), CPU bão hòa trong $\sim 5\,$s -- vừa đúng ngưỡng renew lease của control-plane.

\paragraph{Quan sát ở Cấu hình B (6 vCPU).} Cùng pipeline, cùng pre-flight bypass, được chạy lại trên VM nâng từ 2 lên 6 vCPU. Kết quả hiệu năng các phase ban đầu được ghi nhận ở Bảng~\ref{tab:phase_duration_6vcpu}.

\begin{table}[H]
\centering
\caption{Thời lượng phase đo được ở cấu hình 6 vCPU}
\label{tab:phase_duration_6vcpu}
\begin{tabularx}{\textwidth}{|p{3.5cm}|p{2.5cm}|X|}
\hline
\textbf{Phase} & \textbf{Thời lượng} & \textbf{Trạng thái pod sau phase} \\
\hline
\texttt{cluster} (Kind + Cilium + cert-manager + ingress-nginx) & 265\,s & Toàn bộ pod \texttt{Running}, không CrashLoopBackOff. \\
\hline
\texttt{infra} (Vault + MySQL + Keycloak + Kafka + Kong + ELK + Prom/Grafana) & 630\,s & Không trigger nhánh ``stale failed release'' của script. \\
\hline
\texttt{apps} (8 microservice + Redis sidecar) & N/A (đang chạy)\textsuperscript{*} & Toàn bộ pod \texttt{4/4} sau khi phase hoàn tất. \\
\hline
\multicolumn{3}{|l|}{\textsuperscript{*}Đo từ log thực tế; số liệu sẽ cập nhật khi đo đầy đủ chạy lặp.} \\
\hline
\end{tabularx}
\end{table}

\paragraph{Phân tích đối sánh.} Bảng~\ref{tab:cpu_comparison} tổng hợp khác biệt định tính giữa hai cấu hình.

\begin{table}[H]
\centering
\caption{Đối sánh hành vi cluster ở cấu hình 2 vCPU và 6 vCPU}
\label{tab:cpu_comparison}
\begin{tabularx}{\textwidth}{|p{4cm}|X|X|}
\hline
\textbf{Tiêu chí} & \textbf{2 vCPU} & \textbf{6 vCPU} \\
\hline
Restart \texttt{kube-controller-manager} sau 60 phút & 5 lần & 0 \\
\hline
Restart \texttt{cilium-operator} sau 60 phút & 5 lần & 0 \\
\hline
Restart \texttt{spire-server-0} sau 60 phút & 4 lần & 0 \\
\hline
Trigger nhánh ``stale failed release'' trong \texttt{zta-deploy-spire.sh} / \texttt{zta-deploy-policy-controller.sh} & Có (lặp lại) & Không \\
\hline
Probe \texttt{cilium-operator} \texttt{connection refused} hoặc \texttt{context deadline} & Có & Không \\
\hline
Trạng thái cuối của cluster & Dao động tuần hoàn, không ổn định & Ổn định, mọi pod \texttt{Running}/\texttt{Ready} \\
\hline
\end{tabularx}
\end{table}

\paragraph{Kết luận.} Phép thực nghiệm cho thấy \textbf{ngưỡng CPU tối thiểu của Phase 4 full enforcement trên Kind 4-node là khoảng 6 vCPU cho host VM} ở cấu hình 8\,GiB RAM. Dưới ngưỡng này, cluster không từ chối cài đặt mà rơi vào trạng thái leader-election cascade -- nguy hiểm hơn lỗi cài đặt vì biểu hiện như ``đã chạy nhưng không ổn'' và dễ bị nhầm là bug cấu hình. Pre-flight gate (mục~\ref{subsec:defenses}) chính là cơ chế biến trạng thái cascade ngầm thành cảnh báo tường minh tại thời điểm cài đặt, giúp người vận hành chủ động cấp tài nguyên thay vì chẩn đoán cluster dao động sau khi triển khai. Đây là bằng chứng định lượng quan trọng cho luận điểm: triển khai Zero Trust hoàn chỉnh không chỉ là vấn đề cấu hình policy mà còn là vấn đề kỹ nghệ tài nguyên ở mặt phẳng điều khiển.

\section{Đánh giá mức độ tuân thủ NIST SP 800-207}

Khung tuân thủ sẽ được tổng hợp theo ba nội dung:
\begin{itemize}
    \item \textbf{Bảng đối chiếu 7 Tenets:} ánh xạ từng nguyên lý của NIST SP 800-207 với các cơ chế đã triển khai trong hệ thống.
    \item \textbf{Known Limitations:} ghi nhận minh bạch các giới hạn hiện tại như Kafka plaintext, shared physical DB, HMAC shared key.
    \item \textbf{Kết luận xác thực học thuật:} đánh giá mức độ mà khung lý thuyết ở Chương 2 được kiểm chứng qua case study JOB7189.
\end{itemize}

\section{Thảo luận}

Phần thảo luận tập trung vào ba hướng:
\begin{itemize}
    \item \textbf{Tính tổng quát của khung đề xuất:} khả năng áp dụng cho các hệ Microservices ngoài JOB7189.
    \item \textbf{Khả năng mở rộng trong production:} điều kiện để chuyển từ PoC sang vận hành thực tế.
    \item \textbf{Hướng phát triển tiếp theo:} SPIFFE per-workload certificate, Kafka SASL/SSL, physical DB separation.
\end{itemize}
