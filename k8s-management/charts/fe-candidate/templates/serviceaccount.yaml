apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fe-candidate.serviceAccountName" . }}
  labels:
    {{- include "fe-candidate.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
