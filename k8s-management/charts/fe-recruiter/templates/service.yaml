apiVersion: v1
kind: Service
metadata:
  name: {{ include "fe-recruiter.fullname" . }}
  labels:
    {{- include "fe-recruiter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fe-recruiter.selectorLabels" . | nindent 4 }}
