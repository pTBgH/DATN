apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fe-recruiter.serviceAccountName" . }}
  labels:
    {{- include "fe-recruiter.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
